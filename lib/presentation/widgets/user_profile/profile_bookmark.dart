import 'package:flutter/material.dart';

class ProfileBookmark extends StatelessWidget {
  const ProfileBookmark({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Bookmarks will appear here.'));
  }
}
